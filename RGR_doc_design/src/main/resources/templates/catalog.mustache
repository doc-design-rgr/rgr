<!DOCTYPE html>
<html>
<head>
    <title>Available bouquets</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
</head>
<style>
    html {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        background-color: rgba(99, 166, 255, 0.52);
    }

    body {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        position: relative;
        min-height: 850px;
        font-family: 'Montserrat', sans-serif;
    }

    .container {
        position: relative;
        width: 80%;
        height: 850px;
        margin: 0 auto;
        background-color: white;
        padding: 40px 20px;
        display: flex;
        flex-direction: row;
        justify-content: space-evenly;
        box-sizing: border-box;
    }

    .left_side, .right_side {
        box-sizing: border-box;
        height: 600px;
        padding-top: 20px;
        padding-bottom: 20px;
        position: relative;
        width: 45%;
        border: 2px solid green;
        border-radius: 20px;
    }

    #event {
        display: block;
        margin: auto;
        margin-top: 40px;
        width: 40%;
        border-radius: 10px;
        height: 40px;
        outline: none;
        padding-left: 10px;
        font-size: 18px;
    }

    .order_select {
        display: block;
        margin: auto;
        width: 50%;
        border-radius: 10px;
        height: 40px;
        outline: none;
        padding-left: 10px;
        font-size: 18px;
    }

    h1 {
        text-align: center;
        margin-top: 0;
        color: rgb(0, 86, 37);
    }

    h2 {
        color: rgb(0, 86, 37);
    }

    .search_btn, .submit_btn {
        display: block;
        margin: auto;
        width: 40%;
        min-width: 90px;
        height: 60px;
        font-size: 24px;
        border-radius: 20px;
        background-color: rgba(99, 166, 255, 0.52);
        color: rgb(0, 86, 37);
        border: 2px solid green;
        cursor: pointer;
        outline: none;
    }

    table {
        margin-top: 50px;
    }

    th {
        font-size: 24px;
        padding-bottom: 30px;
    }

    td {
        font-size: 18px;
        text-align: center;
        padding-bottom: 20px;
    }

    .order_form {
        text-align: center;
    }

    input {
        padding-left: 10px;
        height: 40px;
        font-size: 18px;
        border-radius: 10px;
        border: 1px solid rgba(104,165,255,0.98);
        margin-bottom: 30px;
        outline: none;
        width: 50%;
    }
    footer {
        height: 40px;
        background-color: #a69695;
        padding-top: 25px;
        width: 80%;
        margin: 0 auto;
    }
    span {
        display: block;
        margin: 0 auto;
        font-size: 18px;
        text-align: center;
        color: #dbdad9;
    }
</style>
<body>
    <div class="container">
        <div class="left_side">
            <form method="get" action="getBouquetBasedEvent">
                <h1>Choose an event</h1>
                <select name="event" id="event">
                {{#event}}
                    <option value="{{bouquetEvent}}">{{bouquetEvent}}</option>
                {{/event}}
                </select><br>
                <button onclick="hide()" class="search_btn" type="submit">Search</button>
            </form>
            <table align="center" width="100%" height="100px">
                <tr>
                    <th>ID</th>
                    <th>Bouquet name</th>
                    <th>Price</th>
                    <th>Event</th>
                </tr>
                {{#customBouquetBasedFilter}}
                <tr>
                    <td>{{bouquetId}}</td>
                    <td>{{bouquetName}}</td>
                    <td>{{price}}</td>
                    <td>{{bouquetEvent}}</td>
                </tr>
            {{/customBouquetBasedFilter}}
            </table>
        </div>
        <div class="right_side">
            <h1>Make order</h1>
            <form class="order_form" method="post" action="addOrderBouquet">
                <input style="margin-top: 40px" type="number" id="bouquet" name="bouquet" placeholder="Bouquet ID"/><br>
                <input type="text" id="phone" name="phone" placeholder="Confirm your phone number"/>
                <h2>Choose delivery type: </h2>
                <select name="delivery" id="delivery" class="order_select">
                {{#delivery}}
                    <option value="{{deliveryId}}">Type: {{deliveryType}} \ Price: {{deliveryPrice}}</option>
                {{/delivery}}
                </select>
                <h2>Choose packing type</h2>
                <select name="packing" id="packing" class="order_select">
                {{#packing}}
                    <option value="{{packingId}}">Type: {{packingType}} \ Price: {{packingPrice}}</option>
                {{/packing}}
                </select><br>
                <button class="submit_btn" type="choose">Submit</button>
            </form>
        </div>
    </div>
    <footer>
        <span>Розрахуново-графічна робота, НУЛП</span>
    </footer>
</body>
</html>